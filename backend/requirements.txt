# ============================================================
# SmartCycle — Backend Dependencies
# Python 3.9+
#
# This is the MINIMAL dependency set. The project uses
# graceful degradation — every external library has a
# pure-stdlib or mock fallback when unavailable.
#
# To run with FULL capabilities, also install:
#   akshare>=1.14.0       (real-time A-share data)
#   chromadb>=0.5.0       (persistent vector store)
#   sentence-transformers>=3.0.0  (real embeddings)
# ============================================================

# --- Core Framework ---
tornado>=6.5.0
pydantic>=2.10.0
httpx>=0.28.0

# --- Optional: real-time market data ---
# Remove or comment out to use mock data only:
yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0

# --- Development & Testing ---
pytest>=8.0.0
pytest-asyncio>=0.25.0
ruff>=0.8.0
mypy>=1.13.0
